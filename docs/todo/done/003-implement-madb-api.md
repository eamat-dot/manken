# MADBタイトル検索APIを実装

## 背景

- テンプレート移行とMADB仕様調査の完了後、確定した `manken` APIを実装する必要がある
- 外部サービス固有の形式を公開APIへ露出させず、複数のGoプロジェクトと将来のMCPサーバーから利用できる形にする

## 目的

- `docs/spec.md` と `docs/pkg/madb/spec.md` で確定した共通モデルと
  MADBタイトル検索APIを実装する
- 外部通信を行わない単体テストと、明示的な実サービス確認を整備する
- 別のGoモジュールからimportできる状態にする

## 着手条件

- [x] `done/001-migrate-template.md` が完了している
- [x] `done/002-research-madb-api.md` が完了している
- [x] `docs/spec.md` の共通APIに必要な未確定事項が解消されている
- [x] `docs/pkg/madb/spec.md` の初期実装に必要な未確定事項が解消されている
- [x] 実装対象の公開APIについて利用者の承認を得ている

## 非目的

- テンプレートファイルの移行
- MADB仕様の追加調査
- ISBN検索、著者名検索、シリーズ検索
- MADB以外のデータ取得元への対応
- キャッシュ、自動リトライ、レート制限
- CLIとMCPサーバーの実装
- GitHub Releaseとバージョンタグの作成

## 実装項目

### Goモジュール

- [x] `github.com/eamat-dot/manken` をモジュールパスとして `go.mod` を作成する
- [x] 対応するGoバージョンを決める
- [x] 不要な外部依存を追加しない

### mankenパッケージ

- [x] `Source` とMADB用の定数を実装する
- [x] `Book` を実装する
- [x] `SearchBooksRequest` を実装する
- [x] `SearchBooksResult` を実装する
- [x] `ErrorKind` と `Error` を実装する
- [x] 公開要素と名前付き関数へプロジェクト規則に従った日本語のGo Docコメントを書く

### madbパッケージ

- [x] `Client` と `NewClient` を実装する
- [x] `WithEndpoint` を実装する
- [x] 検索条件の検証を実装する
- [x] カーソルの生成と検証を実装する
- [x] SPARQLクエリの生成を実装する
- [x] 全文検索とSPARQLの2段階の文字列エスケープを実装する
- [x] リクエストごとに新しい `http.Request` を作成する
- [x] SPARQL Results JSONの非公開レスポンス型を実装する
- [x] bindingをMADB固有の非公開 `sourceBook` へ集約する
- [x] `sourceBook` から `manken.Book` への変換を実装する
- [x] 複数値の集約、重複除去、順序の安定化を実装する
- [x] Agent名からAuthorsを組み立て、creator文字列のフォールバックを実装する
- [x] ISBN-10とISBN-13のチェックディジット検証を実装する
- [x] HTTPエラーとレスポンス解析エラーの分類を実装する
- [x] レスポンス本文の読込上限を実装する
- [x] コンテキストのキャンセルと期限を維持する

### 単体テスト

- [x] 正常なタイトル検索をテストする
- [x] 0件をテストする
- [x] 欠落項目をテストする
- [x] 複数のタイトル、副題、シリーズ名をテストする
- [x] creatorの役割除去とAgent名の使用をテストする
- [x] 複数のAuthorsとpublisherをテストする
- [x] 複数のISBN-10とISBN-13、不正ISBNをテストする
- [x] 重複bindingをテストする
- [x] Limitの既定値、最小値、最大値、範囲外をテストする
- [x] 初回カーソルと次ページカーソルをテストする
- [x] 不正カーソルと検索条件が一致しないカーソルをテストする
- [x] SPARQLで特別な意味を持つ文字を含むタイトルをテストする
- [x] 全文検索で特別な意味を持つ文字を含むタイトルをテストする
- [x] 不正JSONをテストする
- [x] 代表的なHTTPエラーをテストする
- [x] `Retry-After` をテストする
- [x] レスポンス本文の上限をテストする
- [x] コンテキストのキャンセルとタイムアウトをテストする
- [x] 通常の単体テストから実サービスへ接続しない

### ドキュメント

- [x] READMEへインストール方法と最小の利用例を書く
- [x] 現在利用できる機能だけをREADMEへ記載する
- [x] MADBの名称、公式サイト、利用条件、サービス変更の可能性を記載する
- [x] MCPサーバーが未実装であることを明記する
- [x] 実装結果と `docs/spec.md` を同期する
- [x] 実装結果と `docs/pkg/madb/spec.md` を同期する

## 検証

- [x] `go mod tidy` を実行する
- [x] `gofmt` または `goimports` を実行する
- [x] `go test -v ./...` を実行する
- [x] `go build -v ./...` を実行する
- [x] `golangci-lint run` を実行する
- [x] `git diff --check` を実行する
- [x] 日本語タイトルで実サービス検索を明示的に実行する
- [x] 0件の検索を実サービスで確認する
- [x] 複数ページを重複なく取得できることを確認する
- [x] 単体テスト結果と実サービス確認結果を分けて報告する

## 受け入れ条件

- [x] 別のGoモジュールから `github.com/eamat-dot/manken/madb` をimportできる
- [x] タイトル検索で `manken.SearchBooksResult` を取得できる
- [x] 該当なしの場合に空の結果を取得できる
- [x] MADB固有のレスポンス型とSPARQL用型が公開されていない
- [x] HTTPクライアントとエンドポイントを差し替えられる
- [x] 将来のMCPパッケージが最小インターフェース経由で呼び出せる
- [x] 単体テスト、ビルド、lintが成功する
- [x] 調査事項やテンプレート移行作業をこのTODOへ混在させていない

## 完了結果

- Go 1.26.0、標準ライブラリのみで `manken` と `madb` を実装した
- 通常テストから実サービス確認を `integration` build tagで分離した
- 日本語タイトル、0件、複数ページをMADB実サービスで確認した
- 別のGoモジュールから公開APIをimportできることを確認した
