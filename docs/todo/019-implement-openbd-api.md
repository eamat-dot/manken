# openBD ISBN検索APIを実装

## 背景

- 現在の実装はMADBだけをデータ取得元としている
- openBDは認証なしでISBNから書誌情報を取得できる
- openBDの応答は `summary`、`onix`、`hanmoto` に分かれ、同じ概念の値が
  複数箇所に存在する
- 現在の共通モデルは、取得元の値と安全に正規化した値を分けて保持できる

## 目的

- ISBNを指定してopenBDから1冊の書誌情報を取得できるようにする
- openBD固有の応答を非公開型で受け取り、`api.Book` へ変換する
- 通常の単体テストと実サービス確認を分離する
- 後続の取得元実装で再利用できるパッケージ構造とテスト方法を確立する

## 根拠

- [`../research/012-01-openbd-api.md`](../research/012-01-openbd-api.md)
- [`../research/012-02-book-api-common-fields.md`](../research/012-02-book-api-common-fields.md)
- [`../spec.md`](../spec.md)

## 着手条件

- [ ] ISBN以外の検索条件、`Limit`、`Cursor` を受け取った場合の扱いを決める
- [ ] 該当なし、欠落項目、配列内の `null` に対する結果を決める
- [ ] `summary`、`onix`、`hanmoto` の項目別の採用順序を決める
- [ ] openBD内のIDと参照URLとして使用する値を決める
- [ ] 初期実装で変換する項目と、取得元値だけを保持する項目を決める
- [ ] 決定したopenBD固有契約を `docs/pkg/openbd/spec.md` に記載する
- [ ] 実装対象の公開APIと変換規則について利用者の承認を得る

## 非目的

- タイトル、著者名、フリーワードによるopenBD検索
- 複数ISBNを受け取る新しい公開検索API
- Google Books、楽天、Yahoo!など他の取得元の実装
- 複数取得元の横断検索、結果の統合、重複排除
- 楽天Booksの検索結果をopenBDで補完する処理
- openBD応答の全項目を `api.Book` へ追加すること
- 在庫、送料、重版予定など販売運用情報の共通モデル化
- キャッシュ、自動リトライ、クライアント側のレート制限

## 前提・制約

- 共通型と共通エラーは `api` パッケージに定義する
- openBD固有の応答型と変換規則を公開APIへ露出させない
- openBDパッケージからMADBパッケージをimportしない
- 取得元の値を推測で補完せず、変換できない値は元値またはRaw responseに残す
- ISBNは外部サービスへ送信する前に形式とチェックディジットを検証する
- 通常の単体テストから実サービスへ接続しない
- ログ出力、goroutineの開始、自動リトライ、キャッシュを行わない

## 対象範囲

### 公開契約

- [ ] `api.Source` にopenBD用の定数を追加する
- [ ] `openbd` パッケージから通常利用に必要な共通型、定数、エラーを参照できるようにする
- [ ] `NewClient` と差し替え可能なエンドポイント設定を定義する
- [ ] ISBN検索メソッドを定義する
- [ ] 成功応答本文を返すRaw response用メソッドを定義する
- [ ] 公開要素と名前付き関数へプロジェクト規則に従った日本語のGo Docコメントを書く

### HTTP処理

- [ ] ISBNをクエリパラメーターとして安全にエンコードする
- [ ] リクエストごとに新しい `http.Request` を作成する
- [ ] 呼び出し側が指定したコンテキスト、HTTPクライアント、Transportを維持する
- [ ] 応答本文の読込上限を設ける
- [ ] HTTPエラー、通信失敗、解析失敗を `api.Error` へ分類する
- [ ] 外部サービスの応答本文を通常のエラーメッセージへ含めない
- [ ] コンテキストのキャンセルと期限を標準のエラーチェーンへ維持する

### 応答と変換

- [ ] 初期変換に必要な範囲だけを持つ非公開レスポンス型を実装する
- [ ] 配列の件数、要求ISBNとの対応、`null`、不正な成功応答を検証する
- [ ] openBD固有の非公開値から `api.Book` への変換を実装する
- [ ] 正規化前の主要値を `BookSource.Values` に保持する
- [ ] ISBN、タイトル、巻、シリーズ、寄与者、出版社、日付などを仕様の採用順序で変換する
- [ ] 変換規則が確定した説明、主題、ページ数、判型、価格、画像だけを設定する
- [ ] 寄与者の役割を対応付けられない場合に推測で `Contributors` へ分類しない
- [ ] 欠落した任意項目を有効な応答として扱う

### ISBN検証の共有

- [ ] 既存のMADB用ISBN検証とopenBDで必要な規則を比較する
- [ ] 取得元間で共有する場合は、どちらかの取得元パッケージへ依存しない配置にする
- [ ] ISBN検証の移動によってMADBの公開APIと検索結果を変更しない

### 単体テスト

- [ ] 正常なISBN-10とISBN-13をテストする
- [ ] ISBNのハイフン、Unicode空白、小文字 `x` の正規化をテストする
- [ ] 不正な形式とチェックディジットを外部通信前に拒否することをテストする
- [ ] 該当なしと欠落項目をテストする
- [ ] `summary`、`onix`、`hanmoto` の採用順序をテストする
- [ ] 寄与者、シリーズ、日付などの複数値と役割をテストする
- [ ] 配列件数の不一致、要求ISBNとの不一致、不正JSONをテストする
- [ ] 代表的なHTTPエラー、通信失敗、本文上限をテストする
- [ ] コンテキストのキャンセルとタイムアウトをテストする
- [ ] Raw responseが受信した成功応答本文を変更せず返すことをテストする
- [ ] 通常の単体テストが実サービスへ接続しないことを維持する

### 実サービス確認

- [ ] `integration` build tagを使用するopenBD統合テストを追加する
- [ ] 既知のISBNで1冊取得できることを確認する
- [ ] openBDに存在しない有効なISBNの結果を確認する
- [ ] 欠落項目を含む書籍を取得できることを確認する
- [ ] 実サービスの一時的な応答内容を固定の単体テスト期待値にしない

### ドキュメント

- [ ] `docs/pkg/openbd/spec.md` に検索条件、項目対応、HTTP処理、エラー分類を記載する
- [ ] 実装で確定または変更した共通契約だけを `docs/spec.md` へ反映する
- [ ] READMEにopenBDのISBN検索例と制約を記載する
- [ ] openBDの名称、公式サイト、利用条件、サービス変更の可能性を記載する
- [ ] 実装完了後にバックログの状態とTODOの配置を更新する

## 検証

- [ ] `gofmt` または `goimports` を実行する
- [ ] `go test -v ./...` を実行する
- [ ] `go build -v ./...` を実行する
- [ ] `golangci-lint run` を実行する
- [ ] `go test -tags=integration ./openbd` を実行する
- [ ] `git diff --check` を実行する
- [ ] 通常テストと実サービス確認の結果を分けて報告する

## 受け入れ条件

- [ ] 別のGoモジュールから `github.com/eamat-dot/manken/openbd` をimportできる
- [ ] 有効なISBNで `api.SearchBooksResult` を取得できる
- [ ] 該当なしの場合に仕様で定めた空の結果を取得できる
- [ ] openBD固有の応答型が公開されていない
- [ ] 取得元値と正規化済み値の対応をパッケージ仕様から確認できる
- [ ] HTTPクライアントとエンドポイントを差し替えられる
- [ ] 単体テスト、ビルド、lint、実サービス確認が成功する
- [ ] MADBの公開APIと既存テストが退行しない
- [ ] 非目的に記載した機能を実装へ混在させていない

## リスク・懸念

- 同じ概念が複数セクションに存在し、採用順序を誤ると値の意味が変わる
- ONIXのコード値を不完全に解釈すると、寄与者、日付、価格などを誤分類する
- 出版社ごとに項目の有無と表記が異なる
- openBDはISBN検索専用であり、単独ではタイトル検索の取得元にならない
- 外部サービスのスキーマと提供状況は将来変更される可能性がある

## 未決事項

- ISBN以外の検索条件、`Limit`、`Cursor` の具体的な入力規則
- `summary`、`onix`、`hanmoto` の項目別の採用順序
- 初期実装で変換するONIXコードと、元値だけを保持するコードの境界
- `BookSource.ID` と `BookSource.URL` に設定する値
- ISBNが見つからない場合と不正な成功応答の区別
- Raw responseメソッドの公開名と戻り値

これらは実装前に `docs/pkg/openbd/spec.md` で確定し、承認後に実装する。
