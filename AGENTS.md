# プロジェクト指示書（AGENTS.md）

本プロジェクトの詳細な開発ルールは、`.github/instructions/` 配下の各指示書に定義されています。
タスクを実行する前に、同ディレクトリ内の指示書を確認してください。各ファイルの front matter にある `applyTo` と変更対象を照合し、該当する指示書をワークスペースから読み込んで、その指示に従ってください。
特に、以下の指示書を使用します。

- `.github/instructions/commit-message.instructions.md`
  - すべてのコミットメッセージに適用

- `.github/instructions/writing.instructions.md`
  - すべてのドキュメント類に適用

- `.github/instructions/go.instructions.md`
  - Goコード、`go.mod`、`go.sum` の変更に適用

- `.github/instructions/go-comments.instructions.md`
  - すべてのGoコードの関数コメントに適用
  - `go.instructions.md` の一般的なコメント方針より優先

`docs/todo/` 配下の実装TODOを作成、再開、実行する場合は、`implement-todo` スキルを使用してください。

## AI memory

- 大きな調査、設計、実装、レビューを始める前に、`.ai/memory.md` を読み、過去の設計判断、失敗知識、開発環境上の注意点を復元してください。
- `.ai/memory.md` はAI向けの索引兼キャッシュであり、仕様書、調査記録、完了済みTODO、コード、テストを正本として扱ってください。矛盾する場合は正本を優先し、memory側を更新してください。
- ユーザーからの訂正、繰り返した失敗、有効だったレビュー指摘、非自明な環境・ツール制約、今後も使う標準手順など、再発防止に使える知識が確定した場合は、ユーザーから指示されるのを待たず、その時点でmemoryへの昇格候補を確認してください。
- 大きな調査、設計、実装、レビューの終了時にもmemoryへの昇格候補を再確認し、取りこぼしを防いでください。同じ調査・設計ミス・実装ミス・ツール操作ミスを防ぐ長期的な知識が増えた場合だけ、同じ作業の中で `.ai/memory.md` を更新してください。
- 一時的な進捗、現在のTODO、会話ログ、単発のエラー、正本を読めば自明な仕様はmemoryへ記録しないでください。
- memoryは時系列ログとして追記し続けず、既存項目へ統合し、重複や無効になった知識を整理して小さく保ってください。

この `AGENTS.md` と各指示書の内容が重複する場合は、より具体的な指示を優先してください。

## 既存設計の踏襲

新規provider/package追加時は、実装開始前に既存の類似providerを確認し、公開API・ファイル構成・テスト・Godoc・仕様文書・統合方法を踏襲する

## 構想・アイデア文書の扱い

`idea.md`、`concept.md`、`backlog.md` などに記載された内容は、確定仕様や実装指示とは限らない。

これらの文書をもとに仕様、設計、実装計画を作成する場合は、記載内容をそのまま転記または実装せず、まずユーザーが達成したい目的を整理すること。

次の観点から内容を再評価すること。

- 矛盾、重複、過剰な機能がないか
- 機能や責務を統合できないか
- 処理順序やデータ構造を単純化できないか
- 一般的または確立された手法で代替できないか
- 実装コスト、保守性、拡張性に対して妥当か

独自性の高い案が構造を複雑化する場合は、その案を前提とせず、目的を満たすより単純な代替案を提示すること。

設計上の判断が必要な場合は、一方的に仕様を確定せず、推奨案、代替案、利点、欠点、影響範囲を整理してユーザーに提案すること。

軽微で可逆性が高く、既存方針から自然に判断できる事項については、確認のために作業を中断せず、妥当な案を採用してよい。その場合は、採用した判断を結果または変更内容に明記すること。
