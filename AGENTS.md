# プロジェクト指示書（AGENTS.md）

本プロジェクトの詳細な開発ルールは、`.github/instructions/` 配下の各指示書に定義されています。
タスクを実行する前に、各ファイルの front matter にある `applyTo` と変更対象を照合し、該当する指示書を読んでください。

特に、以下の指示書を使用します。

- `.github/instructions/commit-message.instructions.md`
  - すべてのコミットメッセージに適用
- `.github/instructions/writing.instructions.md`
  - すべてのドキュメント類に適用
- `.github/instructions/go.instructions.md`
  - Goコード、`go.mod`、`go.sum` の変更に適用
- `.github/instructions/go-comments.instructions.md`
  - すべてのGoコードの関数コメントに適用
  - `go.instructions.md` の一般的なコメント方針より優先

この `AGENTS.md` と各指示書の内容が重複する場合は、より具体的な指示を優先してください。

## 既存設計の踏襲

新規provider/package追加時は、実装開始前に既存の類似providerを確認し、公開API、ファイル構成、テスト、Godoc、仕様文書、統合方法を踏襲してください。

## 設計文書の扱い

`docs/concept.md` などの設計文書に記載された内容は、実装または仕様書より優先される確定仕様とは限りません。

これらの文書をもとに仕様、設計、実装計画を作成する場合は、記載内容をそのまま転記または実装せず、まず達成したい目的を整理してください。

次の観点から内容を再評価してください。

- 矛盾、重複、過剰な機能がないか
- 機能や責務を統合できないか
- 処理順序やデータ構造を単純化できないか
- 一般的または確立された手法で代替できないか
- 実装コスト、保守性、拡張性に対して妥当か

独自性の高い案が構造を複雑化する場合は、その案を前提とせず、目的を満たすより単純な代替案を提示してください。

設計上の判断が必要な場合は、一方的に仕様を確定せず、推奨案、代替案、利点、欠点、影響範囲を整理してください。

軽微で可逆性が高く、既存方針から自然に判断できる事項については、作業を中断せず妥当な案を採用して構いません。その場合は、採用した判断を結果または変更内容に明記してください。
